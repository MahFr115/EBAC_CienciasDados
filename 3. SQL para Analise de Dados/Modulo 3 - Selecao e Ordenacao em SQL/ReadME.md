# 🧩 Módulo 3 – Seleção e Ordenação em SQL

Neste módulo, aprofundamos o uso da linguagem SQL aprendendo a **selecionar, filtrar e ordenar dados**.  
Você verá como realizar consultas eficientes, retornar apenas os dados necessários e aplicar filtros e condições para análises mais direcionadas.  

Este é um dos módulos mais práticos da trilha, pois nele você começa a explorar dados de forma ativa, transformando perguntas em consultas SQL.

---

## 🎯 Objetivos do Módulo

✔ Aprender a utilizar o comando **`SELECT`** para consultar dados  
✔ Compreender a cláusula **`FROM`** e a seleção de tabelas  
✔ Aplicar **filtros condicionais** com `WHERE`, `AND`, `OR` e `NOT`  
✔ Utilizar **`DISTINCT`** para eliminar duplicidades  
✔ Implementar **restrições de integridade** como `NOT NULL`, `UNIQUE` e `CHECK`  
✔ Ordenar resultados com **`ORDER BY`** de forma crescente e decrescente  
✔ Organizar consultas complexas com múltiplos critérios de ordenação  

---

## 📂 Conteúdo Abordado

| Tema | Descrição |
|------|-----------|
| Comando `SELECT` | Recuperação de dados de uma ou mais tabelas |
| Cláusula `FROM` | Identificação da tabela ou conjunto de tabelas da consulta |
| Filtros (`WHERE`) | Aplicação de condições simples e compostas |
| Operadores Lógicos | Uso de `AND`, `OR` e `NOT` |
| Eliminação de Duplicidades | Utilização do `SELECT DISTINCT` |
| Ordenação de Dados | Comando `ORDER BY` para resultados organizados |
| Restrições de Colunas | Aplicação de `NOT NULL`, `UNIQUE` e `CHECK` |

---

## 📑 Glossário de Conceitos

Os principais termos técnicos apresentados neste módulo estão detalhados no arquivo **`Glossario.pdf`**, incluindo:  
📎 *Local do arquivo: `./Glossario.pdf`*  

🔎 Entre os conceitos revisados estão:  
`Select Distinct`, `Unique`, `Not Null`, `Check`.

---

## ⚙️ Ferramentas Utilizadas

| Ferramenta | Finalidade |
|-----------|------------|
| **PostgreSQL / MySQL** | Execução das consultas SQL |
| **DBeaver / Azure Data Studio** | Interface para testar e visualizar resultados |
| **Jupyter Notebook** | Integração entre SQL e Python para análises exploratórias |
| **VS Code** | Escrita e documentação de scripts SQL |

---

## 📌 Importância deste módulo

A capacidade de selecionar e ordenar dados é uma das mais essenciais para qualquer analista.  
Neste módulo, você desenvolve o pensamento analítico por trás de uma consulta: **como perguntar corretamente aos dados e obter respostas relevantes**.  
Esses fundamentos serão a base para funções de agregação, junções e análises avançadas nos próximos módulos.